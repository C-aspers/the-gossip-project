<!DOCTYPE html>
<html>
  <head>
    <title>TheGossipProject</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

 <!-- Bootstrap CSS --> 
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

  </head>


  <body>

    <div class="p-4"></div>
    <h1>Gossip n°<%= @gossip_number.to_i + 1 %></h1><br>
    <h3><%= @gossip.title %></h3><br>
    <p><%= @gossip.content %></p>
    <p>created the : <%= @gossip.created_at.to_date %></p>

    <div class="p-4"></div>
    <p>Written by : <%= @user.first_name %> <%= @user.last_name %></p>
    <p>City : <%= @city.name%> </p>

    <% user_id = @user.id %>
    <a class="btn btn-primary btn-sm" href="<%="/user/#{user_id}"%>" role="button">See this creator</a>

  </body>
</html>
